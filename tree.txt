state = {
    count: 20,
    walk: 200,
};

---------------------------------------------------------------------------

_incrementCount = () => {
    this.setState({count: this.state.count + 20});
};

---------------------------------------------------------------------------

<VrButton
    onClick={this._incrementCount}>